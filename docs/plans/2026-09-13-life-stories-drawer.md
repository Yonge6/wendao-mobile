# 生活里的道：抽屉内容栏目

用户要求在抽屉增加栏目收录本次生活场景文章。将原 H5 跳转入口提升为共用组件中的栏目：更多 → 生活里的道 → 文章列表 → 全文 → 对应章节。文章与静态 H5 共用 growth/copy.json，沿用抽屉日夜颜色和阅读字号，英语界面明确正文为中文。返回先到列表再到抽屉首页，关闭保留原阅读位置；章节跳转沿用访问权限，不消耗免费名额。

验收：类型检查、现有构建检查；在窄屏与桌面浏览器验证三个文章、返回、关闭及章节跳转。H5 完成部署并读回，原生共享代码进入后续 App 构建，不把 H5 部署称作 App Store 更新。
